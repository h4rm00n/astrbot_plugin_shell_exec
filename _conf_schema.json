{
  "max_execution_time": {
    "description": "命令执行超时时间（秒）",
    "type": "int",
    "hint": "单个命令的最大执行时间，超过此时间将自动终止命令。",
    "default": 30
  },
  "enable_logging": {
    "description": "是否启用命令执行日志",
    "type": "bool", 
    "hint": "是否记录所有执行的命令到日志文件中。",
    "default": true
  },
  "working_directory": {
    "description": "插件的工作目录",
    "type": "string",
    "hint": "所有 shell 命令将在此目录中执行，并且 URL 下载的临时文件也将存储在此处。如果留空，将默认使用插件目录下的 'workdir' 文件夹。",
    "default": ""
  }
}